[package]
name = "yaps"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = {version = "*", features = ["derive", "unicode", "cargo"]}
serde = { version = "1.0.137", features = ["derive"] }
serde_yaml = "0.8.24"
